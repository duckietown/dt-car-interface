car\_interface package
======================

The `car_interface` package is a wrapper around the all nodes in `car-interface`. It
contains a single launch file that starts all the nodes in `car-interface`.